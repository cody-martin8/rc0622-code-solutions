/* exported getValue */
// receive object and key as arguments
// select value by looking at the key property of the object
// return the selected value

function getValue(object, key) {
  return object[key];
}

// receive object and key as arguments
// select value by looking at the key property of the object
//   ignore error stating dot notation is better
// return the selected value
