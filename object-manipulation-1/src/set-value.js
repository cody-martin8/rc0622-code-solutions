/* exported setValue */
// receive object, key, and value as arguments
// use push method to add property to object
// assign value to property of object
// return object

function setValue(object, property, value) {
  object[property] = value;
}

// receive object, key, and value as arguments
// assign the value to the object property using bracket notation
//   make sure the value is on the right side of the assignment
