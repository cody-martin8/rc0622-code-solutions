/* exported last */
// receive array as argument
// select for last element of array by subtracting 1 from the length of the array
// return selected element

function last(array) {
  return array[array.length - 1];
}

// receive array as argument
// select for last element of array by subtracting 1 from the length of the array
// return selected element
