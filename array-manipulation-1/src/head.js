/* exported head */
// allow function to receive array as argument
// create variable that can receive element as input
//   test whether variable needs to be an array or not
// select first element in array
//   push element into created variable
//   return created variable

function head(array) {
  return array[0];
}

// receive array as argument
// select for first element of array at position 0
// return selection
